/**
 * Enumeration for user deletion process states.
 * Used to track and manage user account deletion statuses.
 */
const Svelters_Shared_Enum_User_Delete_State = {
    ACTIVE: 'ACTIVE',     // Active deletion process
    LOCKED: 'LOCKED',     // Deletion process is locked
};
export default Svelters_Shared_Enum_User_Delete_State;
